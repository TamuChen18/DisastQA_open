# Core ML/AI libraries (for local model evaluation)
torch>=2.0.0
transformers>=4.37.0
accelerate>=0.20.0

# API clients (for closed-source model evaluation)
openai>=1.0.0
aiohttp>=3.8.0

# Utilities
tqdm>=4.64.0
python-dotenv>=1.0.0
